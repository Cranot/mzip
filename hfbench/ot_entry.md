- [ ] **2026-09-02 mzip FAST-path profiling (session dc4c3c43, scratch tree /root/mzip-hfbench/petree; /root/mzip UNTOUCHED):** FAST compress at 64K went 0.64 -> ~50 MB/s (decode 1.0-1.5 GB/s) with output bytes unchanged. Four causes found by measurement, each recorded in /root/mzip-hfbench/MZIP-AT-HF-GRANULARITY.txt RESULT 23-27: (1) compress_impl work buffers sized from the 16 MiB block default (~112 MB zero-filled per call; 443k page faults/279 MB RSS for one 64K chunk); (2) 13-dictionary trial at zstd-19 on every block 256B-256K in every mode (~55 ms/chunk; no dict ever wins a weight block, measured BALANCED-identical with the gate forced); (3) detect_periodic_approx full two-pass scan (~8 ms, 0 hits); (4) compute_fingerprint SimHash on single-block streams (~4 ms, provably unused). Decoder has the mirror buffer bug (2x16 MiB per call; 256K chunks decoded at 15 MB/s). OWNER DECISIONS PENDING: apply (1),(decoder),(4),char_template over-read fix as pure bug fixes; generalise the dict text-gate and periodic prefilter to BALANCED only if the corpora identity run (v5cmp/corpora.*) is identical; PLANE_ENTROPY block type = format addition; commit the 4 earlier tracked changes + train_corpus/hf_dict.bin; the HF reply post (POST-DRAFT-v2.md, not sent).
- [ ] **2026-09-02 mzip pre-existing defects surfaced by ASan/UBSan (repo code, not ours):** heap over-read detect_char_template mzip.hpp:6394 (truncated last line excluded from min_len but indexed in later loops; fixed in petree v5); 8 signed-integer overflows in delta arithmetic: tieredcompress.hpp:249, :556 (0 - INT64_MIN), :1559, :2002, :2003, :2049, :2050, mzip.hpp:3420 (detect_timestamp). Fix shape: unsigned-width subtraction. Also -DMZIP_TIME build segfaults at exit (Dump static outlives table()).
- [ ] **2026-09-02 mzip measurements still in flight at turn end:** quiet final v7 numbers (RESULT 27 has only interleaved-under-load: v7 +8-13% over v6); corpora whole-file identity pre vs v5 (BALANCED+FAST over 8 text corpora, 50 MB) -- decides whether the FAST-only gates can be generalised; v7 BALANCED identity on 4 slices. FAST on small TEXT is still 0.06-0.11 MB/s (fast path covers numeric blocks only) -- a truthful limitation for the post. Deferred speed ideas (each ~10% of the remaining ~1.2 ms/chunk, would change FAST bytes): pick PE plane count k by histogram instead of best-of-2; skip whole-file zstd-1 arm when the block path won; a FAST numeric bypass skipping the strategy trials.
